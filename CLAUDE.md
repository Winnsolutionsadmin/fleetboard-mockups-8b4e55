# UPSTREAM SOURCE OF TRUTH
This repo is a downstream, unlisted PUBLIC mirror of mockups only. The canonical upstream (proposal, research record, unscrubbed mockups) auto-loads via the operator's local ~/.claude registry; deliberately not committed here because this repo is public.
